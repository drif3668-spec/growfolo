# Local development — points to local FastAPI server
NEXT_PUBLIC_API_URL=http://localhost:8000

# Vercel production — next.config.ts auto-sets this from VERCEL_URL.
# You do NOT need to set NEXT_PUBLIC_API_URL in the Vercel dashboard;
# it is resolved automatically to the deployment domain at build time.
